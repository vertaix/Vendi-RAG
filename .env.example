# Copy this file to .env and fill in your keys.
# Never commit .env to version control.

# Required — OpenAI API key (used by VendiRAG for LLM calls)
OPENAI_API_KEY=sk-...

# Optional — only needed if using Gemini-based backbones
GEMINI_API=...
